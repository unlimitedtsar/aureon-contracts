[package]
name = "aureon-contracts"
version = "1.0.0"
edition = "2021"
authors = ["Keystone Team"]
description = "Canonical Data Contracts for the Aureon ecosystem in Rust"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
uuid = { version = "1.0", features = ["serde", "v4"] }
